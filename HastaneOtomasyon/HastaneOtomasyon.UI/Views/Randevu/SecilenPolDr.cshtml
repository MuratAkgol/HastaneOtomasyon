@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860

*@
@using Microsoft.AspNetCore.Http;
@using EntityLayer
@model Anasayfa

@{

}


<div class="container">
    <div class="row">
        <div class="col-md-6 col-sm">
            <form asp-action="RandevuOlustur" asp-controller="Randevu" method="post">
                <p>Randevu almak istediğiniz doktoru seçiniz</p>
                <select class="form-select" aria-label="Default select example" name="DoktorId" id="poliklinikSec" required>
                    @foreach (var item in Model.Doktorlar)
                    {

                        <option value="@item.DoktorId">@item.DoktorAdi&nbsp;@item.DoktorSoyadi</option>
                    }
                </select>
                @foreach (var item in Model.Saat)
                {
                    <input type="radio" name="Saat" value="@item.Saat" />
                    @item.Saat
                    <br />
                }

                <div class="input-box mt-2">
                    <span class="icon"><i class="fa-solid fa-user"></i></span>
                    <label>Hasta TC</label>
                    <input class="form-control" type="text" name="HastaTc" required />

                    <span class="icon"><i class="fa-solid fa-user"></i></span>
                    <label>Hasta Adı</label>
                    <input class="form-control" type="text" name="HastaAdi" required />

                    <span class="icon"><i class="fa-solid fa-user"></i></span>
                    <label>Hasta Soyadı</label>
                    <input class="form-control" type="text" name="HastaSoyadi" required />
                    <br />
                </div>
                <button type="submit" class="btn btn-success mt-2">Devam et</button>
            </form>
        </div>
    </div>
</div>